export const getChatList = state => state.chats.chatList;

export const getProfileName = state => state.profile.name;

export const getState = state => state;

export const getMessageList = state => state.messages.messageList;

export const getGists = state => state.gists;

